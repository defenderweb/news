<%= form_for([@press_release, @press_release.comments.build]) do |f| %>
	<div class="field">
		<%= f.label :commenter %>
		<%= f.text_field :commenter %>
	</div>
	<div class="field">
		<%= f.label :body %>
		<%= f.text_field :body %>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>

<% end %>
