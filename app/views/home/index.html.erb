

<div class="span-11" id="latest_posts">
	<h2>Latest Press Releases</h2>
	<table>
	<% Post.latest_posts.each do |post| %>
		<tr>
		<!-- What's the best method to make this image a link??  -->
		<td><img src="<%= post.brand.icon %>" height="75" width="75" /></td>
		<td>	
			<em><%= post.date.strftime("%B %d %Y")  %></em><br />
			<%= link_to post.title.truncate(150), post %>
			<br />
			<%= link_to 'Edit', edit_post_path(post), :class => "admin" %> 
	    	<%= link_to 'Delete', post, :confirm => 'Are you sure?', :method => :delete, :class => "admin" %>
		</td>
	<% end %>
	</table>
	
	<strong class="fr"><%= link_to "All Press Releases", posts_path %></strong> 
	
	<div class="clear"></div>
	
	<h2>Latest News</h2>
	<em>News articles will eventually be going here</em>
</div>	
	
<div class="span-6 last" id="contact_information">
	<h6>Contact Info</h6>
	<p>
		For press inquiries, contact:<br />
		<% User.marketing_user.each do |marketing| %>
			<strong><%= marketing.name %></strong>
			<a href="mailto:<%= marketing.email %>"><%= marketing.email %></a>
		<% end %>
	</p>
	
	<p>
		<strong>CWD Headquarters</strong><br />
		4080 Montrose Road<br />
		Niagara Falls, ON<br />
		Canada, L2H 1J9
	</p>
	
	
</div>




