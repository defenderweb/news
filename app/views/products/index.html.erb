<h1>Product Presskits</h1>

<table>
  <tr>
    <th>Model</th>
	<th>Title</th>
	<th>Brand</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @products.each do |product| %>
  <tr>
    <td><%= product.model %></td>
    <td><%= product.title %></td>
    <td><%= product.brand %></td>

    <td><%= link_to 'Show', product %></td>
    <td><%= link_to 'Edit', edit_product_path(product) %></td>
    <td><%= link_to 'Destroy', product, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product', 'new' %>
<%= link_to 'PR Home', '/' %>