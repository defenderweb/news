<h1>All Brand Presskits</h1>

<table>
  <tr>
  	<th></th>
    <th>Name</th>
	<th>Parent</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
	<% @brands.each do |brand| %>
	  <tr>
	    <td><img src="<%= brand.icon %>" height="50" width="50" /></td>
	    <td><h3><%= brand.name %></h3></td>
	    <td><%= brand.parent %></td>
	
	    <td><%= link_to 'Show', brand %></td>
	    <td><%= link_to 'Edit', edit_brand_path(brand) %></td>
	    <td><%= link_to 'Destroy', brand, :confirm => 'Are you sure?', :method => :delete %></td>
	  </tr>
	<% end %>
</table>

<br />


<%= link_to 'New Brand Presskit', new_brand_path %>
<%= link_to 'PR Home', '/' %>